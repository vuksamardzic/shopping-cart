import {ShowStepOKPipe} from './show-step-ok.pipe';

describe('ShowStepOKPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowStepOKPipe();
    expect(pipe).toBeTruthy();
  });
});
